# Example Dockerfile - for reference only
# This shows what a Dockerfile would look like, but you may not need it yet

FROM python:3.11-slim

# Set working directory
WORKDIR /app

# Install system dependencies (PostgreSQL client, etc.)
RUN apt-get update && apt-get install -y \
    postgresql-client \
    && rm -rf /var/lib/apt/lists/*

# Copy dependency files
COPY pyproject.toml ./

# Install Python dependencies
RUN pip install --no-cache-dir .[dev,etl]

# Copy application code
COPY finance_app/ ./finance_app/
COPY *.py ./

# Set environment variables
ENV PYTHONUNBUFFERED=1
ENV PYTHONPATH=/app

# Expose port (if you add a web API later)
# EXPOSE 8000

# Default command (can be overridden)
CMD ["python", "-m", "finance_app"]

